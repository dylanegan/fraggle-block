source "http://rubygems.org"

# Specify your gem's dependencies in fraggle-block.gemspec
gemspec

# ensure we use system_timer for development against 1.8.x
group :development do
  gem "system_timer", :platform => :mri_18
end
